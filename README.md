# Overview
Rhythium is a rhythm game created with libGDX, heavily inspired by Osu and Rhythia. 

## Install Dependencies

```
cd "Rhythium X.0"

python3 -m venv .venv

source .venv/bin/activate

pip3 install -r requirements.txt
```

## Run The Program
```
cd "Rhythium X.0"
./gradlew run
```